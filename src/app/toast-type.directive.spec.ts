import { ToastTypeDirective } from './toast-type.directive';

describe('ToastTypeDirective', () => {
  it('should create an instance', () => {
    // const directive = new ToastTypeDirective();
    // expect(directive).toBeTruthy();
  });
});
